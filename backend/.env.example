PORT=5000
JWT_SECRET=your_jwt_secret_here_change_in_production
MONGODB_URI=mongodb://localhost:27017/happyshelf

# Email notifications (low/out-of-stock alerts). If any of these are unset,
# emails are logged to the console instead of sent — the app still works,
# it just won't deliver real mail.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_smtp_password_or_app_password
EMAIL_FROM=HappyShelf <your_email@gmail.com>
